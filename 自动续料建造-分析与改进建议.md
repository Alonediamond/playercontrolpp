# 自动续料建造（Auto Restock Build）— 修复记录与改进建议

> 对应代码：`feature/automaterial/AutoRestockFeature.java`、`feature/automaterial/MarkedContainerManager.java`、
> `integration/BaritoneIntegration.java`、`integration/QuickShulkerIntegration.java`、
> `integration/LitematicaIntegration.java`、`compat/SlotActionCompat.java`
>
> 本轮改动日期：2026-07-28 ｜ 五版本（1.21.1 / 1.21.4 / 1.21.11 / 26.1.2 / 26.2）编译与构建均通过

---

## 一、这一轮修了什么

### 1. 蓝图已经建好时不会自动停止

**根因。** Baritone 的 `BuilderProcess` 在建完后自己调 `onLostControl()`（`BuilderProcess.java:629-640`），
把 `schematic` 置空，于是 `isActive()` 变成 `false`。旧的 `tickMonitoring()` 遇到
「builder 不活跃 + 已经启动过一次」只有一句裸 `return`——功能仍然是「开」的，但永远不再做任何事，
所以必须再按一次热键才能关掉。

**修法。** 监控状态改成：只要建造进程从「活跃」变成「不活跃」，就认定这一轮结束（建完，或者被别的东西取消了），
播报 `build_finished` 并自动停止。同时 `BaritoneIntegration.startLitematicaBuild()` 不再无脑返回 `true`——
`buildOpenLitematic()` 是 `void` 且会吞掉自己的失败（没有对应 placement 时只打一句
"List of placements has no entry"），所以现在它在调用后**回读 `isActive()`** 作为返回值。
这样「启动失败」和「启动成功但瞬间建完」这两件事第一次被区分开了。

### 2. 快捷栏里的潜影盒取不到料

**根因（确定性的索引错位）。** QuickShulker 的 `OpenShulkerPacket` 携带的 `invSlot`，
服务端是这样解析的（`api/Util.java:19`）：

```java
openItem(player, invSlot, player.containerMenu.slots.get(invSlot).getContainerSlot());
```

也就是说这是一个 **InventoryMenu 的「屏幕槽位」下标**，不是 `Inventory` 的物品栏下标。
两套编号只有主背包重合：

| Inventory 下标 | 含义 | InventoryMenu 槽位 |
|---|---|---|
| 0–8 | 快捷栏 | **36–44** |
| 9–35 | 主背包 | 9–35 |
| — | 副手 | 45 |

QuickShulker 自己也是这么算的——`ModKeyCallback.java:25`：`36 + player.getInventory().getSelectedSlot()`。

旧代码直接把 `Inventory` 下标传了进去。主背包（9–35）碰巧对得上，所以「看起来能用」；
快捷栏（0–8）打到的是**合成结果格、2×2 合成格、盔甲格**，`getContainerSlot()` 换算出来是另一个格子的物品，
`QuickOpenableRegistry.getQuickie()` 拿到 null，于是什么都不发生——正好就是你观察到的现象。

**修法。**

- 新增 `QuickShulkerIntegration.menuSlotForInventorySlot(int)` 做换算，调用方统一走它；
- 新增 `isOpenableShulkerBox(ItemStack)`：潜影盒注册时**没有** `ignoreSingleStackCheck`
  （`QuickShulkerMod.java:77-83`），所以 QuickShulker 只开「单个」盒子——数量 ≥ 2 时服务端的
  `forceCloseScreenIfNotPresent` 监听器会在第一次槽位变动时把界面强制关掉。现在这种盒子直接跳过，
  不会表现成「开了一下又没了」；
- 新增 `canOpenFromInventory(Minecraft)`：服务端按 `player.containerMenu` 解析槽位，
  所以**必须在没有其他容器界面打开时**才发这个包。旧代码有一条路径（从箱子里 shift 走一个潜影盒后
  直接切到 SHULKERING）是**开着箱子**去发包的，索引会落到箱子菜单上，取到的是箱子里的东西。
  现在开盒前一律先确认当前打开的是玩家自己的物品栏菜单。

### 3. 明明有材料却莫名其妙停止

这一条有三个独立成因，都修了。

**成因 A：拿「世界里还缺多少」去和「背包里有多少」比大小。**
`MaterialListEntry.countMissing` 是**创建材料清单那一刻**算出来的「世界里还差多少方块」，
而且是 `final`——`MaterialListUtils.updateAvailableCounts()` 只刷新 `countAvailable`，
从来不动 `countMissing`（`MaterialListUtils.java:150-161`）。旧代码把它当成 `neededCount`，
然后取料前要求：

```java
countInInventory(entry.item) < entry.neededCount   // 背包数量 < 世界缺口
```

于是「背包里有 3000 石头、蓝图还差 100 个」这种最常见的情况，条件为假 → 这个材料被判定为「不需要」→
`containerHasAnyNeededItem()` 对每个容器都返回 false → 全部跳过 → 三轮零收获 → `no_progress` 停止。
**玩家明明有材料，功能却停了。**

**修法：换成一个自洽的模型。**

- **target（目标持有量）** = `min(countMissing, 每种材料补给组数 × 最大堆叠)`，新配置项默认 8 组；
- **have（实际可用量）** = 自己数玩家 36 格里的**散装**数量；
- **需要补料** ⟺ `have < target`。

为什么不用 Litematica 的 `countAvailable`：它会把**潜影盒和收纳袋里的东西也算进去**
（`MaterialListUtils.java:176-199`），而 Baritone 只能放散装物品——一盒石头会被算成「石头很充足」，
结果盒子永远不会被打开。

顺带：不再强制要求打开 Litematica 的材料清单 HUD。HUD 只刷新 `countAvailable`（我们已经不用了），
要求它开着纯粹是多一个「莫名停止」的理由。现在只要材料清单存在就能工作，读不到时给的提示也改成了
「请先为该蓝图生成一次材料清单」。

**成因 B：`allowInventory` 默认是关的。**
Baritone 的 `Settings.allowInventory` 默认 `false`（`Settings.java:93`），
建造时只认**快捷栏 9 格**（`BuilderProcess.java:687` 那个 `if`）。
材料躺在主背包里 = Baritone 看不见 = 暂停。这时缺料判定说「不缺」，旧代码会
`restartBuilder()` 无限重启，或者攒够三轮后停止——同样是「有材料却停了」。

**修法：** 新增 `promoteToHotbar()`，用一次「数字键交换」（`ContainerInput.SWAP`，
新加在 `SlotActionCompat.swapWithHotbar()`）把主背包里的材料挪到**空的**快捷栏格子。
只用空格子——挤掉玩家的镐子会让 Baritone 连挖方块都做不到，那是把一个卡点换成另一个卡点。
实在挪不动时（快捷栏满了）给出可执行的提示：腾一个空位，或者把 Baritone 的 `allowInventory` 打开。
另外新增 `BaritoneIntegration.allowsInventory()`，用来在「材料齐全但仍然暂停」时区分两种原因：

- `allowInventory = true` → 剩下的方块八成是够不着 / 被流体挡住 → `paused_unreachable`
- `allowInventory = false` → 材料在背包不在快捷栏且没空位 → `paused_hotbar_full`

**成因 C：潜影盒分支自己会硬停。**
旧 `tickGetItemFromShulker()` 里，只要那一轮 `took == false` 就直接
`active = false; stop("no_progress")`；而开盒失败时 `haveShulkerBoxesContainsItems` 倒数到 0 之后
会**每 tick 刷一次提示且永远不离开该状态**。两者都换成了有界重试 + 正常回到主流程。

### 4. 同一趟顺手修掉的其他问题

| 位置 | 问题 | 处理 |
|---|---|---|
| `tryTransferOne()` Phase 2 | 从箱子里拿走潜影盒后，**开着箱子**就切到 SHULKERING，槽位索引落在箱子菜单上 | 拆成两段：先把盒子收进背包，整趟走完、界面关干净后再开盒 |
| `tickRestarting()` | builder 一直不活跃就永远等下去 | 移除该状态——`startLitematicaBuild()` 现在自带确认，直接回监控；万一是「重启后发现已经建完」也能被正确识别为完成 |
| `neededItems.isEmpty()` 分支 | 无条件 `restartBuilder()`，非缺料型暂停会无限重启 | 改为 `resume()`（不重解析蓝图、不重置层数），并计入无收获轮次上限 |
| 背包满 + 零收获 | `restartBuilder()` 且不计数 → 死循环 | 走统一的 `endCycle()`，计入上限 |
| `stop()` | 不重置 `attemptedBuildLaunch` / `dryRestartCount` / 潜影盒相关标志 | 统一到 `resetRunState()`，一处清干净 |
| `LitematicaIntegration.getIgnoredSet()` | `getDeclaredField("ignored")` 只查具体类，而字段声明在父类 `MaterialListBase` → **一直静默返回空集合**，Litematica 的忽略列表从来没生效过 | 改为沿继承链向上查（`AutoMaterialGatherer` 也一起受益） |
| 取料顺序 | 按 `needs` 顺序取，第一种材料可能把背包塞满 | 每次挑「满足度最低」的那一种取一摞，天然轮转 |
| 取料判定 | 假设 `quickMove` 一定成功 | 比对点击前后的散装数量，真正确认才计入收获 |
| 潜影盒收集 | 箱子里有多少含料盒子就拿多少 | 每轮上限 3 个（拿走的盒子在开盒前一直算「还缺」，不设上限会把整箱盒子搬空） |
| 状态机 | 「取完箱子→开盒→还缺→再跑箱子」可以无限互相甩锅 | `containerTripDone` 标志，每轮最多跑一趟容器 |

---

## 二、现在的运行模型

```
                 ┌──────────────────────────────────────────────┐
                 │                                              │
   热键 ──► 启动建造 ──► MONITORING ──建造进程消失──► 播报「已完成」并停止
                            │
                            └─检测到暂停─► ANALYZING（读材料清单，算 target/have）
                                             │
              ┌──────────────────────────────┼───────────────────────────┐
              │                              │                           │
        needs 为空                    背包里有含料潜影盒              需要跑容器
     （其实不缺料）                          │                           │
              │                        SHULKER_OPEN                startContainerTrip
      挪材料上快捷栏                    SHULKER_TAKE               （此处才 cancelPathing）
              │                              │                           │
              └──────────► resume() ◄────────┘                    PATHING → OPENING
                              ▲                                    → TRANSFERRING
                              │                                           │
                              └────────── FINISHING ◄─────────────────────┘
                                    （挪快捷栏 → resume 或 relaunch）
```

**为什么「续建」有两条路。** 走去标记容器要驱动 Baritone 的 `CustomGoalProcess`，
而 `PathingBehavior.cancelEverything()` 会对所有进程发 `onLostControl()`——`BuilderProcess` 收到后
直接丢掉整个 schematic。所以**跑过容器就必须重新 `buildOpenLitematic()`**。
而从背包里开潜影盒取料**完全不需要移动**，暂停中的建造进程毫发无损，
于是这条路只调 `resume()`：不重新解析蓝图、不把层数重置回 `startAtLayer`、
保留 Baritone 已经算好的 `observedCompleted`。这是这次改动里对「续建流畅度」影响最大的一处。

---

## 三、改进建议

按「收益 ÷ 成本」从高到低排。

### A. 让续料尊重本模组的全局忽略列表 ★★★★★

`Configs.BaritoneSettings.GLOBAL_IGNORE_LIST` / `ENABLE_GLOBAL_IGNORE` 目前只有
`MaterialAnalyzer`（自动搜集功能）在用，续料完全没看。玩家在配置里写了「忽略泥土」，
搜集功能听话、续料功能照拿不误——这是很容易踩到的不一致。

改动量约 6 行：在 `readMaterialList()` 里比照 `MaterialAnalyzer.buildGlobalIgnoreSet()` 过滤即可。
**这次没有直接加，因为它改变的是行为而不是修 bug**，交给你决定。

### B. `schematicIndex` 写死为 0 ★★★★☆

`startLitematicaBuild(0)` 里的 0 是硬编码的。同时加载多个 placement 时，
不管你想建哪个，续料永远建第一个；更糟的是每次重启建造都会**跳回第一个**。

建议：启动时记住当前索引（可以从 `LitematicaIntegration.getAllPlacements()` 里挑
离玩家最近 / 已启用的那个），后续重启都用同一个；或者干脆加个配置项让玩家指定。

### C. 主动刷新材料清单，让 `countMissing` 不再是快照 ★★★☆☆

`countMissing` 只在创建材料清单时算一次。建造推进时它不会变小，所以
「还缺多少」始终偏大——现在靠「每种材料只补 N 组」这个上限把危害压住了，但不精确。

`MaterialListBase.reCreateMaterialList()` 是 public 方法，可以反射调用。代价：

- 它是**异步**的（`TaskScheduler.scheduleTask(task, 20)`），要轮询 `getMaterialsAll()` 直到内容变化或超时；
- 每次都会弹一条 Litematica 的 `scheduled_task_added` 消息，刷屏；
- 大蓝图的重新计数本身要花时间。

所以**不建议每轮都刷**，可以只在按下热键启动那一次刷新一遍。

### D. 用 Baritone 自己的 `getApproxPlaceable()` 交叉验证 ★★★☆☆

`IBuilderProcess.getApproxPlaceable()` 返回 36 格能放出来的 `BlockState` 列表——
这正是 Baritone「我现在能放什么」的第一人称视角，比从 Litematica 清单反推准得多。
可以在「needs 为空但仍然暂停」时用它做二次判据：如果某个还需要的方块状态**不在**这个列表里，
那就是真缺料（或者卡在快捷栏问题上），而不是够不着。

### E. 一轮之内记住「这个容器是空的/走不到」★★★☆☆

现在每轮都会把所有标记容器重跑一遍，包括上一轮已经确认没料的。
在一次 run 内维护一个「本 run 已判定无用」的集合，跳过它们：省掉大量无谓寻路，
`no_progress` 也能更快、更准地触发。

### F. 复用 `automaterial` 包里已有的模块 ★★★☆☆

这个包里已经有一套成熟的实现，是给「自动搜集材料」功能写的：

| 模块 | 能力 | 续料现在的做法 |
|---|---|---|
| `ContainerOpener` (238 行) | 瞄准、重试、目标打不开时回退到相邻方块 | 自己写了一份简化版，只会重试 3 次 |
| `BaritonePathingController` (106 行) | 到达/卡住/失败判定 | 自己写了一份 |
| `ItemTransferExecutor` (312 行) | 散装 + 整盒搬运，带数量上限 | 自己写了一份 |
| `ShulkerBoxStorage` (628 行) | 背包满时把材料装箱腾位置 | 没有，背包满就只能收工 |

续料是这套逻辑的**第二份实现**，两边的 bug 要修两次（这次的槽位换算就是个例子）。
长期建议把续料切到这些模块上；短期至少可以先换掉 `ContainerOpener`——它的相邻方块回退
能解决「箱子上方被雪盖住点不开」这类问题。

### G. 缺料量按「当前层」而不是「整个蓝图」估算 ★★☆☆☆

`IBuilderProcess.getMinLayer()` / `getMaxLayer()` 暴露了 Baritone 正在建的层区间。
把补料目标限制在当前层附近需要的量，比按整个蓝图截断到 N 组更贴合实际，
背包分配也更合理（不会为了 100 层之后才用到的材料占格子）。

### H. 建造完成时不要再 `cancelPathing()` ★★☆☆☆

`stop()` 无条件调 `cancelPathing()`。在「建造已完成」这条路径上建造进程本来就没在跑，
这一下反而会**误伤玩家手动开的其他 Baritone 任务**（比如同时在跑的 `#mine`）。
建议给 `stop()` 加一个「是否需要中断 Baritone」的参数。

### I. 区分「容器打不开」和「容器是空的」★★☆☆☆

服务器上容器可能被领地插件保护。现在两种情况都只是「跳过」，
玩家看到的都是「容器内无所需材料」，会往错误的方向排查。
`tickOpening()` 里重试 3 次仍然没有界面 = 打不开，值得单独提示。

---

## 四、功能添加建议

### 1. ChestTracker 兜底（★ 最推荐）

项目里已经有 `ChestTrackerIntegration.searchItem(Item, BlockPos, range)`，能直接问
「这个物品在哪些箱子里」——`AutoMaterialGatherer` 已经通过 `ContainerSearcher` 在用了。

续料现在只认玩家手工标记的容器。建议：标记容器里找不到某个材料时，退回问 ChestTracker，
把它给出的坐标临时加进本轮队列。玩家不用为每个新箱子都按一次标记键，体验会有质的变化。

### 2. 标记容器的世界内可视化

给标记的容器渲染高亮框 + 悬浮标签（维度、序号、上次取料结果）。
注意：**本项目目前没有任何世界渲染代码**（没有 render 包），需要新接一个
`WorldRenderEvents.AFTER_TRANSLUCENT` 之类的入口，成本不算低，但对「我到底标了哪些箱子」
这个高频困惑收益很大。配置 GUI 里再配一个「点击定位 / 删除」的列表就更完整了。

### 3. 预取式续料（不等暂停就去补货）

现在是「暂停了才去取料」，每次都会中断建造。可以监控散装库存，
低于阈值（比如某材料剩不到 1 组）就在**建造还在跑**的时候主动跑一趟。
需要小心和 Baritone 抢控制权，实现上建议：手动 `pause()` → 取料 → `resume()`，
比等它自己暂停更可控。

### 4. 收工归还：把多余材料存回标记容器

建造完成后一键把剩余建材放回标记的箱子。`ShulkerBoxStorage` 里已经有装箱的骨架可以复用。

### 5. 末影箱支持

QuickShulker 的 `quickEChest` 默认开启，而且末影箱注册时带了
`ignoreSingleStackCheck(true)`（不受单个限制）。玩家背包里揣一个末影箱就等于随身仓库，
对建大工程非常实用，接入成本很低。

### 6. 状态 HUD

一个小面板显示：当前状态机状态、本轮缺料清单（含 have/target）、
已经跑了几轮、下一个目标容器坐标、无收获计数。
现在所有反馈都挤在 actionbar 一行里，一闪而过，出问题时基本没法复盘。

### 7. 跨维度提示

`positionsInCurrentDimension()` 只取当前维度。如果其他维度还有标记容器，
在报 `no_containers` 时顺便说一句「主世界还有 3 个标记容器」，
比单纯说「本维度没有标记容器」有用得多。

### 8. 补料完成后显式走回建造点

现在依赖 Baritone 重启建造后自己重新寻路回去。可以在重启前先记录建造位置并显式 `goto`，
路径更短，也避免重启瞬间 Baritone 从很远的地方重算整个蓝图。

---

## 五、已知限制（不是 bug，是设计边界）

- **只用散装物品**：Baritone 的 `approxPlaceable(36)` 只看 36 格里的散装物品，
  潜影盒内的材料对它不存在。本功能的全部价值就建立在「把盒子里的东西倒出来」这件事上。
- **`countMissing` 是快照**：见建议 C。目前用「每种材料只补 N 组」把偏差控制在无害范围内。
- **单人/服务器差异**：`promoteToHotbar()` 和取料都通过容器点击包实现，
  某些反作弊插件会限制点击频率。现在每次操作之间有 2 tick 冷却，
  如果被踢建议把冷却调大（`transferCooldown = 2` 那几处）。
- **主文档尚未收录本功能**：`PlayerControl++模组详细介绍.md` 里没有自动续料的章节，
  新增的两个热键（标记容器 / 一键建造+续料）和三个配置项也没写进去，建议补上。

---

## 附：本轮新增/变更的配置与提示

**新增配置**（Baritone 标签页）

| 键 | 默认 | 说明 |
|---|---|---|
| `restockStacksPerItem` | 8（1–36） | 每趟取料时每种缺失材料补到多少组 |

**新增提示消息**

`build_finished`（建造结束，自动停止）、`build_resumed`（原地续建）、
`shulker_found`（找到 N 个含料潜影盒）、`shulker_open_failed`、
`hotbar_promoted`（挪了 N 组到快捷栏）、`paused_unreachable`、`paused_hotbar_full`、
`no_material_list`（替代原来的 `no_hud`）
